apiVersion: v1
kind: Service
metadata:
  name: {{ include "inference-app.fullname" . }}
  labels:
    {{- include "inference-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "inference-app.selectorLabels" . | nindent 4 }}
